<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.qa.mapper.CollectMapper">
    
    <select id="findByUserId" resultType="com.example.qa.entity.Collect">
        SELECT id, user_id, question_id, created_at
        FROM collect
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>
    
    <select id="findByUserAndQuestion" resultType="com.example.qa.entity.Collect">
        SELECT id, user_id, question_id, created_at
        FROM collect
        WHERE user_id = #{userId}
          AND question_id = #{questionId}
    </select>
    
</mapper>
